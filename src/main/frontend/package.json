{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.1.1",
    "react-dom": "^16.1.1",
    "react-router": "^4.2.0",
    "react-scripts": "1.0.17"
  },
  "watch": {
    "build": {
      "test": "{src,test}/*.(js|jsx)"
    }
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cp -r build ../resources",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "watch": "npm-watch build"
  },
  "devDependencies": {
    "npm-watch": "^0.3.0"
  }
}
