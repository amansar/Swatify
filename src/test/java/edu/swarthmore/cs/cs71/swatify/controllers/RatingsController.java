package edu.swarthmore.cs.cs71.swatify.controllers;

import edu.swarthmore.cs.cs71.swatify.models.Rating;
import org.junit.Test;

import static org.junit.Assert.assertEquals;

public class RatingsController {
    /*
    @Test
    public void createNewRating() {
        Rating rating = new Rating(1, 5, 2);

        Rating createdRating = RatingsController.createRating(rating);

        assertEquals(rating.getStars(), createdRating.getStars());
    }
    */
}
